# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*

# Editor
.vscode/
.idea/
*.swp

# Local extension build artifacts
*.zip
*.crx
*.pem
__pycache__/

# Environment
.env
.env.*
!.env.example

# Claude personal notes (not committed — private companion to CLAUDE.md)
CLAUDE.local.md

# Raw listing source captures — contain the author's logged-in registration
# view (personal name/session). Never commit to this public repo. The render
# pipeline (listing/render.sh) uses these locally; only the cropped, PII-free
# slides in screenshots/listing/ are published.
screenshots/raw/
